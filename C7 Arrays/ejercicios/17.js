function agregarNumeros(arrayOfNums) {
  // Suma todos los elementos de arrayOfNums y retorna el resultado.
  // Tu código:
  for (let i = 0; i < arrayOfNums.length; i++) {
    return arrayOfNums[i] + arrayOfNums[i];
    
  }
}

module.exports = agregarNumeros;
